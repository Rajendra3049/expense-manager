# Supabase (Project Settings → API) — preferred names
SUPABASE_URL=
SUPABASE_ANON_KEY=

# Alternative (same values; still read by next.config.ts and mapped for the client)
# NEXT_PUBLIC_SUPABASE_URL=
# NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Optional: used as axios default baseURL in lib/http.ts
# NEXT_PUBLIC_APP_URL=http://localhost:3000
