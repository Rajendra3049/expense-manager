# `components/layout/`

Layout **chrome** that appears on multiple routes.

| File | Used by | Notes |
| ---- | ------- | ----- |
| `dashboard-header.tsx` | `app/dashboard/layout.tsx` | Client component: brand link, truncated email on larger viewports, sign-out (uses `useAuth`) |

Add shared footers, sidebars, or mobile nav drawers here as the dashboard grows.
