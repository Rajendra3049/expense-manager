export type CategoryTotalRow = {
  category_id: string;
  category_name: string;
  total_amount: number;
};

export type MonthlyTrendRow = {
  year: number;
  month: number;
  total_amount: number;
};
