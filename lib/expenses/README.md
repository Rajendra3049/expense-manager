# `lib/expenses/`

Validation and small **date helpers** for Phase 1 expense UI (no React).

| File | Contents |
| ---- | -------- |
| `schemas.ts` | `expenseFormSchema`, `categoryFormSchema`, `parseExpenseAmount` |
| `dates.ts` | `toLocalDateString`, `localMonthBounds` for `<input type="date">` and monthly totals |
| `types.ts` | `CategoryRow`, `ExpenseListRow`, `expenseCategoryName()` for joined category display |
