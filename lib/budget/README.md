# `lib/budget/`

Pure helpers for **budget vs spending** UI (no React, no Supabase).

| File | Role |
| ---- | ---- |
| `warnings.ts` | `budgetWarningLevel` (`ok` / `warn` at ≥80% / `over` at ≥100%), `formatBudgetPercent` |
