/**
 * Single source of truth for the app's user-facing name, tagline, and
 * description. Update values here to rebrand across metadata, headers,
 * landing copy, etc.
 *
 * Note: internal identifiers (package name, storage keys, route paths)
 * intentionally retain their original strings to avoid breaking changes
 * for existing users and tooling.
 */
export const APP_NAME = "Hisab Diary";

export const APP_TAGLINE = "Personal finance, simplified";

export const APP_DESCRIPTION =
  "Track expenses, plan budgets, monitor investments, and keep your finances organized — all in one place.";
