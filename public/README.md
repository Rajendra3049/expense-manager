# `public/` — Static files

Files here are served from the **site root** (`/favicon.ico`, `/next.svg`, etc.).

Add images, `robots.txt`, or downloadable templates here. For optimized images in React, prefer `next/image` with assets under `public/` or the App Router conventions.
